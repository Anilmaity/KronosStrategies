[pytest]
# The XAU/USD 90%-winrate research suite lives under tests/.
# Scope collection there so a stray, pre-existing broken test elsewhere
# in the repo (e.g. tick_data_collector/) does not interrupt our runs.
testpaths = tests
addopts = -q
